
INACCEPTABLE (au moins un des points suivants) :
● initiation de plugins uniquement	=> NON
● js inline				=> NON


LÉGER (tous les points suivants) :
● peu de code personnel						=> Beaucoup de code personnel
● au moins un écouteur d'événement (hors DOMContentLoaded)	=> OUI, plusieurs click assets/js/class/HandleForm.js (lignes 66, 90, 119) et dans Toggle.js (lignes 4, 11 et 20) 
● fichier js séparé						=> OUI


OK (tous les points suivants) :
● JS bien découpé (dans plusieurs fichiers si besoin)					=> OUI, je pense
● code commenté										=> OUI
● respect des bonnes pratiques (sélecteurs DOM intelligents, let, const, performances	=> OUI, je penses
friendly...)
● ne pas bloquer de fonctionnalités si le JS est désactivé (ex : formulaire toujours utilisable)  => OUI, sauf fonctionnalitées type Toggle spécifique au js etc


BIEN (au moins deux des points suivants) :
● utilisation de ES6			=> OUI, je pense (utilisation de const assets/js/main.js lignes 11 à 17, synthaxe `${function}` assets/js/class/Price.js lignes 78 & let ligne 87  + utilisation de class)
● AJAX					=> OUI (voir assets/js/class/AjaxRequest.js et assets/js/class/Api.js)
● local storage				=> OUI (voir assets/js/class/Theme.js ligne 18 & assets/js/class/Api.js ligne 15 et 20)
● JSON					=> OUI (voir assets/js/class/Theme.js ligne 21 & assets/js/class/Api.js ligne 20)
● manipulation du DOM sans jquery	=> OUI (aucun jQuery et beaucoup de manipulation du DOM, exemple assets/js/class/Statement.js lignes 8 à 40)
● API HTML5				=> Plusieurs API voir assets/js/class/Api.js, mais je ne sais pas si HTML5
● POO					=> OUI, voir le dossier class dans assets/js/class ainsi que le main.js


TB (au moins un des points suivants) :
● fonctionnalités utiles (ex : formvalidator)				=> OUI formvalidator (voir class assets/js/class/HandleForm.js) + fonction utile d'estimation rapide (voir class assets/js/class/Price.js)
● fonctionnalités originales (ex : animation poussée, mini jeu)		=> NON
● un gestionnaire de package genre NPM					=> NON, npm utilisé pour sass uniquement
