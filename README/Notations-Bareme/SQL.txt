
INACCEPTABLE (au moins un des points suivants) :
● injections sql							=> NON, je ne penses pas
● sql dans le html							=> NON
● dénomination des champs invalide (espaces, caractères spéciaux, etc…)	=> NON, je ne penses pas
● mot de passe en clair ou hash non sécurisé (md5, sha1)		=> NON (password_hash)
● interclassement invalide ou non pertinent				=> NON, je ne penses pas


LÉGER (tous les points suivants) :
● Dump						=> OUI 
● Présence d’un CRUD complet fait à la main	=> OUI	(voir les models src/models)
	- SELECT (simple)			=> OUI
	- INSERT				=> OUI
	- UPDATE				=> OUI
	- DELETE				=> OUI
● utilisation de PDO				=> OUI (voir src/Database.php ligne 23)
● requêtes préparées				=> OUI (voir src/models/Model.php lignes 30, 45, 64 etc etc...)


OK (tous les points suivants) :
● stockage mot de passe (hash)		=> OUI (password hash, voir src\controllers\Subscribe.php ligne 117 & Update.php ligne 67)
● utilisation de jointure		=> OUI (INNER JOIN, voir src/models/Sanction.php  ligne 21 et 40)
● diagramme UML				=> OUI, je penses


BIEN (tous les points suivants) :
● Quelque chose de plus que login / messagerie (au moins 4 tables)		 => JE SAIS PAS faut compter etc
	○ (et au moins un des points suivants) :
		● gestion du SQL dans un fichier indépendant genre database.php  => OUI (connexion pdo dans src/Database.php et requetes SQL découpées dans les models src/models)
		● ORM + un CRUD à la main obligatoire				 => NON


TB (au moins un des points suivants) :
● colonnes calculés/groupes			=> OUI
● utilisation des vues				=> NON
● statistiques (fonctions d'agrégation)		=> NON
● pas d'utilisation du *			=> NON, j'en ai utilisé dans les models (le model parent)
● requêtes imbriqués				=> NON